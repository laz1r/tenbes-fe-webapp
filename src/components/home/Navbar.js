const Navbar = () => {

    return ( // JSX template

        <nav className="navbar">

            <h1>Tenbes</h1>

            <div className="links">
                < a href="/" >Profile</a>
                < a href="/">Logout</a>
            </div>

        </nav>
    );
}

export default Navbar;